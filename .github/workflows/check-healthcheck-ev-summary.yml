name: Check Healthcheck EV Summary

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  healthcheck-ev-summary:
    name: Healthcheck EV Summary
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: STEP 87 smoke check
        run: |
          echo "workflow file recognized"
          echo "STEP 87 CHECK: OK"
